<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".Doctor_physical_appt">

    <!--Back Button-->
    <ImageButton
        android:id="@+id/btn_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="25dp"
        android:background="#FFFFFF"
        app:srcCompat="@drawable/back_icon" />

    <!-- Title Physical Appointment -->
    <TextView
        android:id="@+id/title_physical_appt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp"
        android:text="Physical Appointment List"
        android:textSize="24sp"
        android:textStyle="bold" />

    <!-- No Appointment Yet Indicator  -->
    <TextView
        android:id="@+id/tv_no_appt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textSize="20sp"/>

    <!-- All Physical Appointment List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/physical_appt_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/title_physical_appt"
        android:layout_centerVertical="true"
        android:layout_marginTop="25dp"
        android:padding="10dp" />

    <!-- Progress Bar -->
    <ProgressBar
        android:id="@+id/progressBar_physical_appt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="650dp"
        android:visibility="gone" />

</RelativeLayout>